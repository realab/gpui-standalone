[project]
name = "zed-gpui-sync"
version = "0.1.0"
description = "Mirror selected GPUI crates from the Zed repository"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
zed-gpui-sync = "zed_gpui_sync.cli:main"

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
